# Dockerfile para Desenvolvimento - Cooperados API
FROM openjdk:21-jdk-slim

# Informações do projeto
LABEL maintainer="Cooperados Team"
LABEL version="1.0.0-dev"
LABEL description="API de Cadastro de Cooperados - Ambiente de Desenvolvimento"

# Define o diretório de trabalho
WORKDIR /app

# Instala dependências do sistema
RUN apt-get update && apt-get install -y \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

# Copia o arquivo pom.xml primeiro para aproveitar o cache do Maven
COPY pom.xml .

# Instala o Maven
RUN apt-get update && apt-get install -y maven

# Copia o código fonte
COPY src ./src

# Define variáveis de ambiente para desenvolvimento
ENV JAVA_OPTS="-Xmx1g -Xms512m"
ENV SPRING_PROFILES_ACTIVE="dev"
ENV MAVEN_OPTS="-Xmx1g"

# Expõe a porta da aplicação
EXPOSE 8081

# Healthcheck para desenvolvimento
HEALTHCHECK --interval=30s --timeout=3s --start-period=60s --retries=3 \
    CMD curl -f http://localhost:8081/actuator/health || exit 1

# Comando para executar em modo desenvolvimento
CMD ["mvn", "spring-boot:run", "-Dspring-boot.run.jvmArguments='-Xmx1g -Xms512m'"]
